using UnityEngine;
using TMPro;
public class CanvasControl : MonoBehaviour
{
    /*[SerializeField] private TextMeshProUGUI deathTitle;
    [SerializeField] private GameObject deathImageBackground;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        if (deathTitle != null)
        {
            deathTitle.gameObject.SetActive(false);
        }
        deathImageBackground.SetActive(false);
    }

    public void Die()
    {
        deathTitle.gameObject.SetActive(true);
        deathImageBackground.SetActive(true);
    }
    // Update is called once per frame
    void Update()
    {

    }*/
}
